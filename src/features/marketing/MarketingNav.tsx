"use client";

import Link from "next/link";
import { useEffect,useRef,useState } from "react";

const links=[["#about","आमच्याबद्दल"],["#purpose","उद्देश"],["#clusters","समूह"],["#coverage","कार्यक्षेत्र"],["#membership","सभासदत्व"],["/register","नोंदणी"],["/register/focused-value-chain","मूल्य साखळी सभासदत्व"],["#contact","संपर्क"]];

export function MarketingNav(){const toggle=useRef<HTMLButtonElement>(null);const[scrolled,setScrolled]=useState(false);const[open,setOpen]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>40);onScroll();window.addEventListener("scroll",onScroll,{passive:true});return()=>window.removeEventListener("scroll",onScroll)},[]);useEffect(()=>{if(!open)return;const close=(event:KeyboardEvent)=>{if(event.key==='Escape'){setOpen(false);toggle.current?.focus();}if(event.key==='Tab'&&window.matchMedia('(max-width:767px)').matches){const links=Array.from(document.querySelectorAll<HTMLAnchorElement>('#marketing-navigation a'));const first=links[0];const last=toggle.current;if(event.shiftKey&&document.activeElement===first){event.preventDefault();last?.focus();}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first?.focus();}}};document.querySelector<HTMLAnchorElement>('#marketing-navigation a')?.focus();document.addEventListener('keydown',close);return()=>document.removeEventListener('keydown',close);},[open]);useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow=""}},[open]);return <header className={`marketing-nav${scrolled?" scrolled":""}`}><div className="marketing-nav-inner"><nav id="marketing-navigation" className={open?"open":""} aria-label="Primary navigation">{links.map(([href,label])=><Link href={href} key={href} onClick={()=>setOpen(false)}>{label}</Link>)}<Link href="/login" onClick={()=>setOpen(false)}>लॉग इन</Link></nav><button ref={toggle} aria-controls="marketing-navigation" className={`hamburger${open?" active":""}`} aria-expanded={open} aria-label={open?"Close menu":"Open menu"} onClick={()=>setOpen((value)=>!value)}><span/><span/><span/></button></div></header>}
